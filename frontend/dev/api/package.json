{
  "name": "dax-dev-api",
  "version": "0.2.0",
  "private": true,
  "description": "DAX dev SWA API — passes user's Power Platform access token + agent connection string to the browser SDK.",
  "engines": {
    "node": ">=20"
  }
}
