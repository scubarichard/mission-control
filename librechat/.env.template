# LibreChat environment variables — template
# Copy to .env and fill in per-client values (or inject via Container App secrets)

# ---------- App ----------
HOST=0.0.0.0
PORT=3080
ALLOW_REGISTRATION=false
ALLOW_SOCIAL_LOGIN=true

# ---------- Azure OpenAI ----------
OPENAI_API_KEY=__from_keyvault__
AZURE_OPENAI_API_INSTANCE_NAME=__client_instance__
AZURE_API_VERSION=2024-08-01-preview
AZURE_OPENAI_MODELS=gpt-4o

# ---------- Entra ID (OpenID Connect) ----------
# Deploy-EntraApp.ps1 creates these in Key Vault and wires them into the Container App.
OPENID_ISSUER=https://login.microsoftonline.com/__CLIENT_TENANT_ID__/v2.0
OPENID_CLIENT_ID=__managed_by:Deploy-EntraApp.ps1__
OPENID_CLIENT_SECRET=__managed_by:Deploy-EntraApp.ps1__
OPENID_SCOPE=openid profile email
OPENID_CALLBACK_URL=/oauth/openid/callback
OPENID_BUTTON_LABEL=Sign in with Microsoft

# ---------- Session ----------
# Deploy-LibreChatSecrets.ps1 creates these in Key Vault and wires them into the Container App.
JWT_SECRET=__managed_by:Deploy-LibreChatSecrets.ps1__
JWT_REFRESH_SECRET=__managed_by:Deploy-LibreChatSecrets.ps1__
CREDS_KEY=__managed_by:Deploy-LibreChatSecrets.ps1__
CREDS_IV=__managed_by:Deploy-LibreChatSecrets.ps1__

# ---------- MongoDB ----------
MONGO_URI=__from_keyvault__

# ---------- Domain ----------
DOMAIN_CLIENT=https://__container_app_fqdn__
DOMAIN_SERVER=https://__container_app_fqdn__
